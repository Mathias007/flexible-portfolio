import { FC } from "react";

const ContactTitle: FC = () => {
    return (
        <div className="section-title padd-15" data-testid="contact-title">
            <h2>Contact Me</h2>
        </div>
    );
};

export default ContactTitle;
