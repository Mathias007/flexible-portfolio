import { FC } from "react";

const PortfolioTitle: FC = () => {
    return (
        <div className="row" data-testid="portfolio-title">
            <div className="section-title padd-15">
                <h2>Portfolio</h2>
            </div>
        </div>
    );
};

export default PortfolioTitle;
